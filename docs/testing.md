# Test coverage plan

## Baseline and scope

Updated 2026-09-18: `Package.swift` defines `VaktaCoreTests` and
`VaktaIntegrationTests` targets; `.github/workflows/ci.yml` runs `swift test`
(509 tests as of this update) plus `Tests/scripts/test_*.sh` (the release
version/signing/install scripts) on every push/PR, alongside `swift build`
and the generated Xcode app build. Every ticket in the tables below is
closed under kata epic `vakta#mkwv`, or is a subsequent follow-up
(`vakta#3cwd`, `#52mr`, `#2bwd`, `#daxg`, `#43sg` under epic `vakta#63w2`) --
the tables describe what was actually implemented and where its coverage
lives, not future work. See [README.md](../README.md#contributing) for the
exact commands.

This plan remains the reference for what a NEW behavior change in one of
these areas needs to keep covering; it is not a queue of unstarted work. Add
a row (or extend an existing one) when a change adds a new decision or
effect boundary, per "Coverage evidence and maintenance" below.

Implementation must follow the phase gates in [AGENTS.md](../AGENTS.md).

## Test layers and harness

| Layer | What it proves | Isolation |
| --- | --- | --- |
| Functional core | Decisions, parsing, migration, identity, and state transitions | Value inputs/outputs; supplied time/IDs; no application launch, files, process execution, or OS services |
| Imperative shell | Correct boundary behavior and resulting state | Temporary files, real fixture subprocesses, stateful in-memory adapters, real Combine/AppKit where feasible |
| Desktop integration | Surface lifetime, focus/input, rendering, native notifications | Logged-in macOS desktop with Metal and the pinned Ghostty dependency |

Homes: `Tests/VaktaCoreTests`, `Tests/VaktaIntegrationTests`, and the manual
"Desktop regression checklist" below (no automated desktop suite exists;
Metal-backed surfaces need a logged-in session -- see AGENTS.md's "Terminal
invariants"). Keep test code out of the application target. Isolate core
code in an importable module as needed; do not launch `VaktaMain` to test
decisions.

Start with XCTest compatible with the declared tooling. Core tests should avoid
AppKit/Ghostty imports. Nondisplay integration tests can run on macOS CI without
constructing Metal-backed surfaces. Give each test its own temporary storage and
owned resources, clean them up even on failure, and bound waits with diagnostics.
Avoid real user settings, existing multiplexer sessions, network endpoints, and
notification permissions in automated unit/integration tests.

`swift test` must execute a nonzero test suite in CI (it does: 445 tests as of
this update). Keep the existing SwiftPM and generated-Xcode build checks. Add
an Xcode test action only when the generated scheme has actual test targets
(it doesn't yet -- `swift test` covers `VaktaCoreTests`/`VaktaIntegrationTests`
directly). Document display prerequisites
and explicit skips; never count skipped desktop checks as passing coverage.

## Priority 1: data preservation and runtime correctness

| Area / issue | Functional core cases | Shell / integration cases |
| --- | --- | --- |
| Helper execution and PATH — `mc9g` | PATH extraction with noise/empty output; fallback choice; typed success/error outcomes | Output larger than pipe capacity; nonzero exit; invalid encoding; missing executable; timeout/cancellation; child ignoring termination; descendant holding stdout open; bounded cleanup; responsive launch |
| Polling/discovery lifecycle — `6g3c` | Accept only current generations/live IDs; retain prior status on failure; invalidate removed/edited targets; derive badge from accepted state; `LaunchTargetResolver.agentStatusPollOutcome` as `pollAgentStatus`'s poll/unavailable/skip selection predicate, derived from `statusArgv != nil` rather than the herdr command name (`vakta#2bwd`) | Controlled completion order; repeated timer ticks; remove session during pending poll; stop/start ownership; no unbounded in-flight work; no resurrected session status. `pollAgentStatus`'s per-poll session selection itself isn't independently unit-tested — same `SessionStore` 0%-coverage rationale as its other wiring; verify via the desktop regression checklist |
| Profile server identity — `b2ry` | Local/remote/backend/socket target resolution; same name on different endpoints; unsupported command classification; JSON/text response fixtures | Temporary executable reports argv/environment; custom executable/PATH used; independent same-named sessions; empty success distinguished from query failure |
| Launch planning and validation — `h4q9` | Placeholder quoting contexts; spaces, quotes, substitutions, semicolons, newlines, leading dashes; scrub/environment key validation; malformed editor lines; values containing `=` | Execute against harmless argument dumper in temp directory; injected marker never created; child environment scrubbed while parent stays intact; working directory honored |
| Persistence — `k916` | Round trips and migrations for profiles, workspace, appearance, sidebar, terminal, notifications, keybindings, passthrough; missing fields; unknown enums/future versions; malformed and extreme values. Appearance file (`AppearanceFileCodec`) decodes a file predating `sidebarFontSize` with the size defaulted to `0` (follow the terminal) and keeps an out-of-range size rather than rejecting the file (validated at use). Sidebar file (`SidebarSettingsFileCodec`) migrates the legacy bare `SidebarCollapseStyle` string to the `SidebarSettings` struct (collapse style + collapsed flag + expanded width), defaults fields a partial/older file omits, and decodes an out-of-range width rather than rejecting it (clamped at use) | Absent vs unreadable/corrupt; preserve original bytes; atomic saves; write failure surfaced; no silent temp-root fallback; isolated storage; default seed and migration restart behavior. `SidebarSettingsStore` seeds/restores the collapsed flag and expanded width, persists each on mutation, and legacy-migrates then round-trips the new struct across a restart (unknown-style file falls back in memory without overwriting bytes). `AppearanceStore` persists `sidebarFontSize` across a restart, seeds `0` on first launch, and a pre-`sidebarFontSize` file keeps its theme/style on the next save |
| Workspace recovery — `njjm` (depends on `k916`) | Restore ordering, selected ID, missing profiles, empty workspace, working-directory overrides; transient command override excluded from durable state | Restore multiple records without partial file rewrites; interrupted restore preserves recovery data; deleted profile never silently launches another command; restart round trip |
| Agent attention — `vsn0` | Empty/mixed agent sets; aliases/unknown statuses; working plus waiting retains attention; baseline suppression; selected/frontmost suppression; repeat suppression; completion transition; independent banner/bounce settings | Parse real response fixtures; in-memory notification state; stale session activation; packaged-app permission denial, delivery failure, click activation, and Dock behavior |
| Workspace disclosure (local only) — `hyac`, neutralized to backend-agnostic naming by `daxg`, tmux windows added by `43sg` | `workspaceListArgv`/`workspaceFocusArgv` construction for both herdr and tmux (table-driven: default/`-S`/`-L` socket addressing); `LaunchTargetResolver.supportsWorkspaces` as the single disclosure/⌘K-switcher capability gate, true for both backends now that tmux vends argv (`vakta#3cwd`, `#43sg`); `WorkspaceQuery.parse` decode into the neutral `Workspace` value, dispatching on backend -- herdr's `result.workspaces[]` JSON (ignoring unknown keys, empty list, malformed/error payload) and tmux's `|`-separated `list-windows` lines (empty output, a malformed/short line dropped without failing the batch, a `|` inside a window name staying intact); `HerdrPreferences` default-off and missing-field migration; fetch-on-expand's stale-completion guard (`WorkspaceFetchPlanner.shouldApply`) drops a result for a session removed while the query was in flight; `PaletteItemAssembler.assemble(workspaces:)`/`PaletteCategory.workspace`/`PaletteItemKind.focusWorkspace` and `SessionSwitcherModel.replaceWorkspaces`'s row filter, all backend-neutral (`vakta#daxg`); `WorkspaceFocusPlanner.applying` -- the optimistic local `focused`-flag flip `SessionStore.focusWorkspace` applies immediately (an unknown/stale id clears every flag rather than leaving one stale) so the sidebar highlight moves without waiting on `fetchWorkspaces`' next completion; `WorkspaceRefreshGate.shouldTrigger` -- the passthrough-input gate (event kind, session focus, `supportsWorkspaces`, `HerdrPreferencesStore.showWorkspaces`) behind `WorkspaceRefreshMonitor`'s re-fetch, since tmux has no event stream to subscribe to (docs/multiplexer-backends.md) and herdr's own `workspace.*` events are unverified (docs/herdr-events-plan.md's Follow-ups); `WorkspaceRefreshDebouncer.scheduledFireDate` -- trailing debounce with a minimum-interval floor so a typing/click burst collapses to one fetch without starving updates during a long burst | Fixture executable threads the target's executable/environment through `workspace list`/`workspace focus` (herdr) and `list-windows`/`select-window` (tmux; deliberately no `switch-client` -- see docs/multiplexer-backends.md); a real (not fixture) tmux server run against `WorkspaceQuery.workspaces` with `ProcessRunner`'s actual minimal environment catches the `|`-vs-tab locale regression the fixture-script tests can't reach; nonzero exit reported as failure; `HerdrPreferencesStore` first-launch seed, corrupt-file recovery, immediate persist on toggle. `SidebarView.supportsWorkspaces`/`AppDelegate.supportsWorkspaces` are one-line delegates to `LaunchTargetResolver.supportsWorkspaces` with no independent decision logic, so herdr-vs-tmux-vs-shell disclosure/switcher-row behavior is verified via the desktop regression checklist rather than a separate UI-model test; `SessionStore.fetchWorkspaces`/`focusWorkspace` wiring, and `WorkspaceRefreshMonitor`'s `NSEvent` monitor itself, are likewise checklist-verified only, matching `SessionStore`'s existing 0%-coverage rationale. Known gap, accepted for v1: a workspace switch driven from outside Vakta (another attached tmux client, a script calling `select-window`) isn't caught by the passthrough trigger. Scoped to the local herdr CLI socket only for herdr — confirmed remote machines (`herdr machine list`) aren't reachable this way; see `hyac` |
| Herdr event subscriptions — `s5n3` (see `docs/herdr-events-plan.md`) | `HerdrSocketPath` session-name→socket resolution; `MultiplexerTarget.eventStreamSocketPath` as the single event-stream capability gate (herdr only, nil for tmux — `vakta#52mr`); `HerdrPaneRegistry` pane-set change detection; `HerdrSocketDecoder` frame classification (ack/pane event/other/malformed) and newline-framing (split-across-reads, multiple-per-read); `HerdrAgentStatus.query`/`paneIDs` decode of real `agent list` JSON | `HerdrEventStreamClient` against a real fixture Unix socket server (`FixtureHerdrSocketServer`): subscribes on connect, triggers on a relevant frame but not an ack, reconnects with the full updated pane set on `updatePaneIDs` (no live patching — confirmed unsupported by the real protocol), reconnects after a server-dropped connection, `stop()` doesn't reconnect. `SessionStore` wiring (`startHerdrEventClientIfApplicable`'s client-per-herdr-session lifecycle now gated on `eventStreamSocketPath` rather than the command name, debounced trigger → `pollAgentStatus`) isn't independently unit-tested — see `SessionStore`'s existing 0%-coverage rationale; verify via the desktop regression checklist |

Use controlled input time and explicitly delivered asynchronous results for race
tests. Assert final public state and effects represented as data; a fake that merely
counts calls is an interaction mock, not a state-based substitute.

## Priority 2: application behavior and delivery

| Area / issue | Functional core cases | Shell / integration cases |
| --- | --- | --- |
| Profile editing and menus — `ht12` | Edit/cancel validation; stable identity; menu projection; deleted/final-profile policy | Menu and sidebar reflect add/edit/delete; stale menu action cannot launch old command; discovery invalidated; persisted default survives restart |
| Sidebar publication — `nqtc` | Width for expanded/icons/hidden combinations; `SidebarWidthCapture.accepted` records a user-dragged width only when expanded, finite, and in range (rejects collapsed/programmatic and sub-minimum drags, clamps above the maximum). `SidebarTerminalGlyphs` -- terminal style's text glyphs: ▾/▸ disclosure, a workspace's ○/✓/· status column (never blank, so labels stay aligned), and a session row's trailing ●/✓ or nothing when there is no agent to report on. `SidebarRowFontResolver.fontSize(sidebarOverride:matchingTerminal:)`/`hasExplicitSize` -- an explicit Appearance sidebar size wins, `0`/invalid follows the terminal. `SidebarRowPresentation` -- `workspaceRowStyle` (prominent = accent fill + marker + strong label, strictly stronger than subtle, than none) and `SidebarTerminalGlyphs.workspaceFocusMarker` (▌/▏/space); only the selected session's focused workspace is `.prominent` (another session's is `.subtle`), and a status mark shows only for statuses that report on an agent. tmux workspace rows parse `@vakta_last_exit` as unknown/zero/nonzero and render a muted/green/red circle | Actual Combine emissions drive current divider width; hidden-to-icons and icons-to-hidden update immediately; expand via menu/shortcut; preserve surfaces. Restored-collapsed sidebar applies the collapsed width on the first `$isCollapsed` emission (no expanded-width flash); restored-expanded resumes at the persisted width |
| Shortcut rules — `560r` | Exact relevant modifiers; conflict replacement; session-index bounds; legacy migrations; cleared defaults remain cleared; focus/capture routing | Unmatched terminal keys pass through; accepted app action once; atomic rebinding persistence; recording cancelled on context loss; normal field editing and input-method composition |
| Command registry — `docs/command-registry-plan.md` (ticket pending; kata unavailable) | `AppCommandCatalog.paletteCommands` keeps the pre-registry 18 ⌘K rows' order, ids and titles; `stableID` unique across `bindableCommands`; `bindableCommands` is exactly the chord ∪ palette union (includes `.nextUnreadSession`, previously missing from Preferences); `CommandAvailability` gates the eight multiplexer commands on `supportsSelectedSessionActions` and nothing else; registry-added commands are `contextSensitive`; fixed pre-rename v7 bytes decode and re-encode to the same JSON shape; an unknown command case makes the file undecodable rather than partially loaded; palette rows carry `.command(_)` with `action:<stableID>` ids (`AppCommandCatalogTests`, `KeybindingFileCodecTests`, `PaletteItemAssemblerTests`, `PaletteNavigationPlannerTests`) | A registry-added binding survives a matcher restart; a legacy v7 file's bytes are untouched at startup; an unknown-case file is recovered in memory and not overwritten; a chord bound to a registry-added command is consumed and delivered, and falls through under a real `NSTextView` (`KeybindingPersistenceTests`, `KeybindingMatcherRoutingTests`). `AppDelegate.perform(_:)`'s availability guard (chord on an unavailable command is consumed without effect), the palette's filtered rows, and the grouped Preferences list are desktop-checklist only |
| Leader keys — `docs/command-registry-plan.md` follow-up (ticket pending; kata unavailable) | Default `LeaderTree` (leaves are bindable commands, unique sibling keys, no empty groups, no clipboard/close-window leaves, Doom-style paths); `LeaderSequencePlanner.step` (group descends, leaf commits, ⎋ cancels, ⌫ backs up or cancels at root, unknown/modified key cancels, device-only flags ignored, unavailable leaf or all-unavailable group cancels, stale path cancels); `LeaderHintAssembler` rows (tree order, unavailable leaves/groups hidden, `SPC`/`TAB`/lowercase glyphs; `s 1…9` and `TAB 1…9` titled with session/workspace names, digits with nothing behind them hidden, the focused workspace marked current); `CommandAvailability` gates `selectSession(i)`/`focusWorkspace(i)` on a session/workspace at that position; `LeaderNode.commandPaths` and `LeaderHintAssembler.sequence(for:)` give each command its sequence, which `PaletteItemAssembler` attaches to ⌘K rows and `PaletteMatcher` matches exactly (spaces/case ignored, `tab n` spelled out), ranked ahead of title matches, with partial sequences not matching (`PaletteMatcherTests`, `PaletteItemAssemblerTests`); `LeaderSettings` decode defaults (off, ⌘/, 300 ms hint delay; a file predating the delay field decodes to 300), round trip, out-of-range delay kept raw but clamped to 0–2000 ms at use, default chord collides with no shipped binding (`LeaderKeyTests`) | Real `NSEvent`s through `KeybindingMatcher.handle`: flag off falls through; leader consumed and pending; `w v` delivers `.splitPaneRight` and `TAB 2` delivers `.focusWorkspace(1)` once with every key consumed; ⎋/unknown/unavailable-group consumed and cancel; leader chord falls through under a real `NSTextView` and in passthrough; `flagsChanged` keeps the sequence; entering passthrough, disabling the flag, or `cancelLeader()` returns to idle; `setLeaderChord` unbinds a same-chord binding and `setBinding` refuses the leader chord; settings (including the hint delay) survive a restart while the pending path does not; a corrupt `leader.json` is not overwritten (`KeybindingMatcherLeaderTests`). The which-key panel (`LeaderHintOverlay`: delay, placement, non-key, theming, redraw when a workspace fetch lands), the workspace fetch on leader start, the ⌘K row's sequence hint and its absence while leader keys are off, `perform(.focusWorkspace)` resolving against the cached list, the Preferences Leader Key section, and cancel-on-deactivate are desktop-checklist only |
| Passthrough — related to `560r` | Modifier down/up sequences with supplied timestamps; threshold boundaries; unrelated keys/modifiers interrupt double-tap; disabled toggle; switching toggle resets pending gesture; mode starts off | Real flagsChanged/keyDown routing; enter/exit passthrough; preference persists but runtime mode does not; capture precedence defined; indicator follows emitted state without one-event lag |
| Session switcher — related to `560r`, `eers` | Whitespace/case filtering; empty results; wraparound navigation; query resets highlight; stale/removed selection ID rejected | Open/reopen; Enter/Escape/click; focus restored after dismissal; modifier navigation and marked-text input; no duplicate commit; selected row scrolls into view |
| Effective terminal settings — `1148` | Finite/ranged font size before integer conversion; missing font/theme; consistent fallback; emitted configuration retains keybind clearing | Persist/reload; live font/theme changes update all surfaces and chrome without recreation; invalid saved values cannot crash preference rendering |
| Session/surface lifecycle — `eers` | Close selected/nonselected/last; fallback selection; duplicate close; rejected close; title precedence; rename trimming | Pinned close action accepted/rejected; deferred teardown; one controller; stable surface identity across switching; visible/focused session agrees with selection; owned timers/monitors stop |
| Herdr config editor — `docs/herdr-config-gui-plan.md` | `HerdrConfigDocument` surgical patching (existing/new keys, new tables, top-level placement, CRLF, inline comments, unknown keys and `[[keys.command]]` untouched, multi-line arrays surfaced `.raw`, unset of an unterminated last line); `HerdrConfigSavePlanner` decision order (invalid, then external edit, then unverified); `HerdrConfigDiagnostic.parse` of real `herdr config check` output; `HerdrConfigLocator`/`HerdrConfigFingerprint`; `HerdrConfigCatalog` invariants (unique paths, defaults valid under their own validator, only the documented restart key flagged); validator, text-input parsing, `HerdrConfigFieldState` (absent/default vs set-in-file vs wrong-shape read-only) and `HerdrConfigSaveMessage`; `HerdrKeyChord` grammar (prefix/modifiers/ranges/named punctuation, canonical formatting, malformed rejection), `HerdrKeyActionCatalog` defaults (each parses; no default conflicts), per-action rules (prefix key, navigate-mode restrictions, empty only for unset-by-default), `HerdrKeyConflicts`, `HerdrKeyBindingState`/`Input`; theme color layers (optional colors accept empty = unset); `[[keys.command]]` array-table patching (append/remove/set/unset per block, separator-blank handling, no leakage into `value(at:)`) and `HerdrCustomCommand` rules; `HerdrSidebarRows` parse/format/validate (plain tokens only); `HerdrKeyChord.fromKeyEvent` (key code + produced characters + modifiers to herdr spelling; shift kept for letters/digits/named keys, dropped for punctuation; named punctuation; round-trips through `parse`); `HerdrConfigSearch` (all-words match over label/key path/help/group, effective key bindings, catalog order) | Fixture `herdr` executable + temp dir: backup then atomic write (symlink-resolved, backup failure aborts), pruning, `config check` on a temp copy (live file untouched; exit 127 = unavailable), reload success/failure, `HerdrConfigStore` save gate (invalid rejected with file+edit intact, external edit conflict, unverified confirmation, reload failure still saved, write failure stays dirty, raw replace). `HerdrKeyRecorder` (capture via the matcher's monitor, ⎋ cancels, drops on window resign) and live-commit of text fields are desktop-checklist only. The SwiftUI pane, ⌘K "Edit/Reload Herdr Config" actions, and real-`herdr` reload coverage for `[keys]`/`[theme]` are verified via the desktop checklist only |
| Release/install — `9w8w` | Valid/invalid tag/manual versions; no/partial/full signing field sets with synthetic values | Validate generated bundle/artifact version; workflow branches; staged install failure preserves prior app; use temporary app roots, never production credentials or `/Applications` in tests |
| Unread-pane navigation and bell — `apdk`, `rcnw` | `UnreadAttentionPolicy.shouldMarkUnread` against an arbitrary `trackedStatuses: Set<AgentStatus>` (deliberately independent of notifyOnAttention/bounceDock; first observation counts as unread -- a session already blocked/waiting on reattach must still surface, confirmed live: a workspace can be `.attention` while a *different* workspace sharing the same herdr session has focus); `UnreadTrackingSettings.trackedStatuses` derivation from its four toggles; `NextUnreadSessionPlanner.next` wraparound; `HerdrAgentStatus.panes` per-pane decode (workspace id, cwd-derived label, herdr's own `focused` flag, missing-field fallbacks) | `SessionStore`'s per-pane wiring (`applyPaneUpdates`: mark/opportunistic-clear composing session selection with herdr's per-pane `focused`, `focusUnreadPane`, `applicationDidBecomeActive`, `paneStatusByWorkspaceID` for the disclosure rows' own status) and the bell popover aren't independently unit-tested -- same constraint as the rest of `SessionStore`/`SidebarView`; `UnreadTrackingSettingsStore` first-launch seed/corrupt-file recovery/immediate persist, matching every other settings store; verify via the desktop regression checklist |

| Mutating multiplexer actions — ticket pending | `MultiplexerTarget.actionArgv` construction (capability #8, docs/multiplexer-backends.md), table-driven per action (split right/down, close/zoom/resize/rename pane, workspace create/rename/close, stop session) × backend × socket flag (default/`-S`/`-L`): herdr's explicit-id CLI shape, tmux's `-t`-targeted commands, split-direction and resize-direction→flag mapping, resize's per-backend baked step (herdr `0.05` ratio vs tmux `5` cells), `createWorkspace` label present/absent, names/labels threaded as data; `LaunchTargetResolver.supportsActions` as the ⌘K action-row capability gate (herdr/tmux true, plain shell and herdr-`--remote` false), mirroring `supportsWorkspaces` (which gates the sidebar's equivalent menu, since it lives only on workspace rows) (`MultiplexerActionTests`) | `MultiplexerCommand.run` against a fixture executable threads the target's executable/environment/socket into the child argv and reports a non-zero exit as failure (`MultiplexerCommandShellTests`). `SessionStore.performAction`/`performPaneAction` (off-main dispatch, cross-backend active-pane resolution via `PaneQuery`, `refetchTopology`'s `WorkspaceFetchPlanner.shouldApply` stale guard), the sidebar workspace-row context menu, and the ⌘K action rows are checklist-verified only, matching `SessionStore`/`SidebarView`'s existing 0%-coverage rationale — including the one assumption not unit-testable here: that an explicit-id mutation works from `ProcessRunner`'s scrubbed PATH/HOME env outside a herdr pane (the already-shipping `workspace focus`/`pane list` exercise the same addressing) |
| File sidebar — ticket pending | `DirectoryListing.sorted` ordering/visibility (directories before files, case-insensitive alphabetical within each group, dotfiles hidden unless `showHidden`, empty input) (`DirectoryListingTests`); `FileTreeLayout.visibleRows` -- the Files tree flattened depth-first into lazily rendered rows (root children at depth 0, only loaded and expanded directories descended, expanded-but-unloaded shown empty, `/` root not doubling the separator) (`FileTreeLayoutTests`); `FileSidebarPreferences` decode/migration (missing fields default to hidden/260, round-trip) and `clampedWidth` (extreme/non-finite clamped at use, raw value preserved on decode) (`FileSidebarPreferencesCodecTests`); `FileRowClickPlanner.action` -- rows act on each click as it arrives (no SwiftUI single-vs-double tap delay): a directory toggles on the first click only, a file opens on the second, a deleted file never opens, a triple click doesn't reopen (`FileRowClickPlannerTests`). Changes mode (`vakta#0nv9`): `GitStatusParser.parse` of `git status --porcelain=v1 -z` bytes (modified/added/deleted/untracked, AD/RD read as deleted, a rename's or copy's source record consumed even when not UTF-8, a non-UTF-8 path skipping only its own record, unmerged pairs as conflicts, ignored and short records skipped, names with spaces/arrows verbatim, no trailing NUL); `GitChangeScope.scoped` to the root's `--show-prefix` (no sibling-stem match); `GitChangeTree.build` (directories first, case-insensitive, dotfiles shown), `visibleRows` (depth-first flattening with depths, collapsed directories' contents skipped) and `directoryPaths`; `GitStatusQuery.interpret` (exit 128 = not a repository, other failures = unavailable, clean = empty, deleted files and untracked directories left out, only the terminating newline trimmed from the prefix, one non-UTF-8 path not hiding the rest) (`GitChangesTests`); `mode` decode (missing/unknown → files) and `FileSidebarModePlanner.togglingChanges` (hidden → visible Changes; Files ↔ Changes while visible) (`FileSidebarPreferencesCodecTests`) | `GitStatusQuery.query` against a real `git` in a temp repository with isolated HOME/no system config: modified/renamed/new files reported with the deleted file and untracked directory left out, a subdirectory root scoped and relativized, no `index.lock`/index rewrite (`--no-optional-locks`), the same result under `status.showUntrackedFiles=no` and `=all`, a symlinked root, a linked worktree, a changed submodule as one modified row, clean repo, plain directory with `GIT_CEILING_DIRECTORIES` as not-a-repository, git off PATH as unavailable (`GitStatusQueryShellTests`); `FileSidebarChangesLoader` with a manual background scheduler: at most one query in flight with later requests collapsed into one follow-up for the latest root (a different root's result dropped), sustained same-root refreshes still publishing each answer, clear-then-new-root restarting, the snapshot carries its prebuilt tree, a nil root clears and drops the in-flight result, a same-root refresh keeps the previous list until the new one lands; `FileSidebarPreferencesStore` mode survives a restart, a pre-mode file keeps its other values, `apply` sets visibility and mode together (`FileSidebarChangesTests`). The header toggle, badges, the flat lazy row list, and the single re-query per root refresh (`fileSidebarRefreshed`, also sent when the root clears) are checklist-verified; click delivery through `NSApp.currentEvent.clickCount` (trackpad, VoiceOver activation with no mouse event → treated as one click) is likewise desktop-only. `DirectoryReader.children` against a real temp directory (names + directory flags, a symlink-to-directory reported as a directory via `fileExists(isDirectory:)`, `nil` for a missing path or a plain file) (`DirectoryReaderShellTests`). `FileTreeModel` against an in-memory filesystem and a manual background scheduler: root children sorted without dotfiles, expand shows children, a prefetched subfolder expands with no wait, collapse/re-expand needs no read, an earlier root's result dropped, a root change forgets expansion, reload re-reads expanded directories and keeps them open, an unreadable directory expands empty, clearing empties the tree (`FileTreeModelTests`). `FileSidebarPreferencesStore` first-launch seed/corrupt-recovery/immediate persist, `SessionStore.refreshFileSidebarRoot`'s off-main resolve + `selectedID`-guarded publish, and the right `NSSplitView` pane's show/hide + width persistence (`SeamlessSplitView.onFileSidebarDragEnded`) are checklist-verified only, matching `SessionStore`/AppKit-chrome's existing 0%-coverage rationale. Live filesystem watching is out of scope for v1 — the tree refreshes on selection, on reveal, and via the header refresh control (a herdr `cd` produces no outer OSC-7, so it needs a re-query) |
| Open in Editor — ticket pending | `MultiplexerTarget.activePaneWorkingDirectoryArgv` per backend (capability #7, docs/multiplexer-backends.md); `ActivePaneWorkingDirectoryQuery.parse` -- herdr's `pane current` envelope (success, `server_not_running`, other error codes, malformed/empty), tmux's single-line `#{pane_current_path}` output; `WorkingDirectoryResolver.resolve`'s multiplexer→OSC-7→profile-launch-directory precedence, including an empty OSC-7 string treated as unknown; `OpenInEditorPlanner.plan`'s `.auto` resolution order (project marker → `$EDITOR`/`$VISUAL` → first installed in a fixed list, each step falling through when it names an editor that isn't installed) and every failure outcome (`.noWorkingDirectory`/`.directoryMissing` checked before editor resolution, `.noEditorInstalled` for an uninstalled explicit choice); `EditorPreferences`/`EditorChoice` decode-missing-field migration and round trip | `ActivePaneWorkingDirectoryQuery.query` against a fixture executable, including the regression case that a non-zero exit with herdr's error envelope on stdout still yields `.serverNotRunning` rather than `.malformed` (confirmed live: herdr exits 1 with the error JSON on stdout for `server_not_running`) -- this is why the query is built on `BoundedProcessRunner.runRaw` rather than `ProcessRunner`, which discards stdout on any non-zero exit. `SessionStore.resolveOpenInEditor`'s snapshot/dispatch/stale-completion-guard wiring, `EditorLaunchAdapter`'s `NSWorkspace` installed-editor lookup and launch, and the ⌘K action/Session-menu item/Preferences picker are checklist-verified only, matching `SessionStore`'s existing 0%-coverage rationale -- see the desktop regression checklist |
| Pane working directories — `4y81` (epic `jmeh`) | `PaneQuery.parse` → `Pane.workingDirectory`/`workspaceID`: herdr `foreground_cwd` over `cwd`, empty or missing → nil, `workspace_id` kept for session-wide listings; tmux's `MultiplexerTarget.tmuxPaneFormat` (`\u{1f}`-separated id, window, active, path, title -- title last with a bounded split so a separator or `\|` in the title stays intact; `\|`/non-ASCII in the path preserved; empty path → nil; malformed/short/bad-active/empty-id lines dropped individually); `paneListArgv`/`sessionPaneListArgv` per backend, tmux with `-u` and socket flags (`PaneQueryTests`, `LaunchTargetTests`) | Real tmux server under `ProcessRunner`'s PATH/HOME-only environment (`PaneQueryShellTests`): a pane in `a\|b café` titled `editor\|with pipe` reports both exactly -- regression for tmux rewriting the separator and non-ASCII bytes to `_` without a UTF-8 locale (verified to fail with `-u` removed); the session-wide listing covers every window with its workspace id. Panes run `sleep` so a shell prompt can't race the title |
| Per-repo PR index — `rkhp` (epic `jmeh`) | `PullRequestIndex.parse` of a real-shaped `gh pr list` array (CheckRun + StatusContext rows, `reviewDecision` `""`): required fields per entry (an incomplete entry dropped alone), older-gh optional fields defaulted, non-array/garbage nil, `isComplete` false once the count reaches `--limit`; `pullRequest(branch:headOwner:)` matches head owner case-insensitively so a fork's same-named branch doesn't match; `PullRequestReview` maps the three GitHub decisions, empty/unknown → nil; `PullRequestChecks.summarize` keeps roux's worst-of rollup (failing > pending > passing > none; neutral/skipped pass; every failure conclusion; unknown/incomplete pending; status-context `state`; case-insensitive); `PullRequestListQuery.arguments` (explicit `--repo`) and `interpret` (launch failure/127 → unavailable, 4 → not authenticated, other exit/timeout/cancel → failed, bad JSON → malformed) (`PullRequestIndexTests`) | Fixture `gh` script on a temporary PATH (`PullRequestListQueryShellTests`): success parses and records the exact argv; exit 4 → not authenticated; gh absent → unavailable; exit 1 → failed; a hanging gh is cut off by the timeout. The argv and the unauthenticated exit code 4 were checked once against real gh 2.97.0; tests never use the real gh, auth, or network |
| Repo/branch keys for PR lookup — `axxf` (epic `jmeh`) | `GitRemote(url:)` (scp-style, `ssh://` with user/port, https with user, `git://`, `.git`/trailing slash, host lowercased, enterprise host kept; local paths, `file://`, and non-`owner/name` paths nil) and `ghRepository` (host only off github.com); `RepoCheckout.parseConfig` of `git config -z` records; `pullRequestTarget` -- head remote by git's push precedence (branch `pushRemote` > `remote.pushDefault` > branch tracking remote > `origin`), PR repository is a same-host `upstream` else the head repository, no target when detached, remote missing, local, or host unsupported; `PullRequestTargetPlanner.plan` dedupes panes into targets and repositories (no checkout root in the key, so worktrees share one `gh` call); `RepoCheckoutCachePlanner.plan` resolves new and stale directories only and prunes unused ones (`RepoCheckoutTests`) | Real `git` in temporary repositories with isolated HOME/no system config (`RepoCheckoutQueryShellTests`): a subdirectory resolves root, branch, and remotes to the expected target; a linked worktree has its own root/branch; detached HEAD has no branch; an unborn branch is still named; a `git checkout` shows on the next query; a plain directory under `GIT_CEILING_DIRECTORIES` and git off PATH are not checkouts |
| PR status store — `852j` (epic `jmeh`) | `PullRequestRefreshPlanner.repositoriesToFetch` (never fetched due; focused repository after `focusedMaxAge`, others after `backgroundMaxAge`; `forceFocused` refetches only the focused repository; gh missing/logged out backs off to the background age unless forced; a transient failure retries on the normal schedule), `applying` (success replaces the index and clears the failure; a failure keeps the last good index), `pruned`; `PullRequestStatusProjection` -- pane targets joined to cached indexes, the focused pane's `FocusedPullRequestState` (branch even without a PR; nil when nothing focused/supported), `workspaceSummaries` over distinct PRs with failing/changes-requested counts and `needsAttention`, workspaces without a PR omitted (`PullRequestStatusPlanningTests`); session-wide tmux listing flags only the active window's active pane (`LaunchTargetTests`) | `PullRequestStatusStore` against a stateful in-memory world (panes, checkouts, gh answers), a manual scheduler, and a controllable clock (`PullRequestStatusStoreTests`): publishes the focused PR and workspace summary; branch-only focus without a PR; nothing for non-repository panes; a session closed mid-cycle leaves no entry; a failed fetch keeps the previous status; fresh cache not refetched while `forceFocused` refetches immediately; TTL expiry refetches; a pane moving repositories is followed; requests during a cycle collapse into one follow-up. Real tmux session-wide listing marks one focused pane (`PaneQueryShellTests`). `SessionStore` wiring (20 s timer, refresh on select/removal, forced refresh on `applicationDidBecomeActive`, helper PATH/HOME) is checklist-verified, matching `SessionStore`'s existing rationale |
| Status bar — `9hpx` (epic `jmeh`) | `StatusBarPreferences` decode (missing/unknown → Automatic) and round trip; `StatusBarVisibility.next` cycle and `needsPullRequestStatus` (only Never skips lookups); `StatusBarPresentation.content` (empty without focus, branch-only, PR number/url/title/draft, `attentionElsewhere` = distinct PRs needing attention across the session's workspaces minus the focused one), `glyph` worst-first (failing > changes requested > pending > passing/approved > none), `isDocked` per mode (Auto-hide never docks); `PullRequestSummary.needingAttention` counts a PR failing with changes requested once; `StatusBarAutoHide` (dwell reveals, a quick pass doesn't, the bar area counts only once revealed, stays while hovered, hides after the delay, re-entry cancels, peek reveals then hides, hover takes over from a peek, `nextDeadline`); `StatusBarDockPlanner` (Automatic docks at once, undocks only after continuous emptiness, flapping restarts the wait; other modes immediate); `toggleSummon` (reveals for `summonDuration`, a second press dismisses any reveal, hover holds a summoned bar); deadlines compared as dates, not recomputed intervals (a 0.15 s dwell missed its own deadline in floating point); `StatusBarPeekPolicy` (settled state change of the same PR or a rising attention count; never on first observation, a return to pending, or another PR); command titles/stable IDs/palette/leader `o b` (show briefly)/`o v` (cycle)/`o r` (`StatusBarTests`, `StatusBarBehaviorTests`, `AppCommandCatalogTests`) | `StatusBarPreferencesStore` first-launch seed, persistence and `cycle` across a restart, corrupt file left untouched (`StatusBarPreferencesStoreTests`). The bar's hosting in the terminal wrapper, docking frame change, Auto-hide overlay fade and `StatusBarHoverStrip` tracking, timers, and click-to-open are desktop-checklist only (step 11) |
| Onboarding: welcome tour and What's New — `xm2w` | `AppVersion` parsing (`v` prefix, missing patch, prerelease, invalid/unexpanded `$(MARKETING_VERSION)`) and semver ordering (`AppVersionTests`); `OnboardingPlanner.presentation` -- fresh install shows the tour even with an unknown version, upgrade from a pre-onboarding build or unparseable last-seen shows only the latest note up to the running version, recorded upgrade shows notes in (last-seen, current] newest first, same/downgrade/no notes/unknown version/unusable file show nothing; `recordedState` never moves last-seen backward; `priorInstallDetected` ignores `onboarding.json` and dotfiles; `releaseHistory` for the on-demand command; shipping catalog versions unique with highlights (`OnboardingPlannerTests`); `OnboardingState` decoding (`OnboardingStateCodecTests`); `ExecutableSearch.firstMatch` (first directory wins, empty/relative entries skipped), `MultiplexerSetupPlanner.status` (installed wins; brew → `brew install <tool>`; no brew → herdr's install script, tmux needs Homebrew) and `installerProfile` quoting (`MultiplexerSetupPlannerTests`); `WorkspacePayloadBuilder` never persists a transient session or its selection (`WorkspacePayloadBuilderTests`); `TutorialShortcut.label` chord → leader-sequence-when-enabled → none, rebound chords, `TutorialContent.steps` order/shortcuts, `TutorialNavigation` (`TutorialContentTests`); palette/bindable rows for Show Welcome Tour / What's New (`AppCommandCatalogTests`) | `OnboardingLaunch` against a temp root (`OnboardingLaunchTests`): fresh root shows the tour and writes nothing until acknowledged, unacknowledged tour reshown, existing install sees What's New then nothing after acknowledge + restart, nothing-to-show records the version immediately, corrupt `onboarding.json` shows nothing and is never overwritten. `evaluate` runs before `Stores` seeds defaults (desktop step 10). Install step: `ExecutableSearch.locate` against a temp dir (executable file found, non-executable file and same-named directory ignored) and the installer profile's command line run through a real `/bin/sh` with `true`/`sh -c 'exit 3'` standing in for the install (echoes the command, reports success/`failed (exit 3)`, exits on Return) (`MultiplexerSetupShellTests`); `MultiplexerSetupModel`, the panel, and the transient installer session are checklist-verified |

## Desktop regression checklist

Run this after changes to terminal hosting, lifecycle, keyboard handling, or live
configuration. Record the macOS/toolchain/app revision and which steps were checked.

1. Launch both a packaged app and the documented plain-shell development mode in
   a desktop session. Check startup responsiveness and useful failure reporting.
2. Create at least three sessions. Produce ongoing output in one; switch using
   sidebar, shortcut, switcher, and status menu. Check surface identity and output
   continuity as well as visible focus.
3. Collapse to icons, change to hidden, then expand. Drag the divider to resize the
   expanded sidebar. Resize the window and change font/theme. Check traffic-light
   clearance and retained terminal input/output. Quit and relaunch; confirm the
   sidebar resumes its collapsed/expanded state and its last dragged width. Switch
   the sidebar font between System and Terminal Style: terminal style shows tight
   rows, ▾/▸ disclosure, text status glyphs, tmux command-status circles, and square full-width highlights;
   its Sidebar font size stepper resizes rows live and "Match terminal" follows
   the terminal size. In both styles only the selected session's focused workspace
   is strongly highlighted, labels are vertically centered in the highlight, and
   a session without an agent shows no status dot.
4. Exercise normal terminal keys, bound/unbound shortcuts, recording cancellation,
   passthrough toggles, text-field editing, non-US layout, and input-method composition.
5. Close selected/nonselected/last sessions, including natural child exit and rapid
   repeated requests. Relaunch and verify the intended workspace recovery.
6. With disposable agent sessions, check mixed working/waiting status, notification
   toggles, permission denial, click activation, and removal while a poll is pending.
7. Run "Open in Editor" (Session menu and ⌘K) against a herdr session, a tmux
   session, and a plain shell profile, with the Editor preference set to
   "Automatic" and to an explicit editor. Confirm it opens the focused pane's
   actual directory, not the session's launch directory, after `cd`-ing
   inside the pane. Confirm each failure outcome surfaces visibly: no editor
   installed, no working directory resolvable, and a directory removed out
   from under the session.

8. Right-click a workspace row on a herdr session and a tmux session: run
   Split Right/Down, Zoom Pane, Resize Pane ▸ (each direction), Close Pane,
   Close Workspace, New Workspace, and Rename… (inline field: Enter commits,
   Esc cancels, whitespace-only is dropped). Confirm each changes the live
   layout and the sidebar/⌘K disclosure refetches to match (no stale rows).
   From ⌘K also run Rename Pane… (prompt), and Stop Session from the session
   row's menu and ⌘K (confirm the server session ends and the row is removed,
   distinct from Detach which leaves it re-attachable). Repeat
   the pane and workspace actions from ⌘K against the selected session's
   focused workspace. Confirm a plain-shell session offers none of these.

9. Enable Appearance ▸ "Show file sidebar" (and ⌘K "Toggle File Sidebar").
   Confirm a right pane appears with a tree of the selected session's
   focused-pane cwd; expanding a folder loads its contents lazily; double-click
   opens a file; right-click reveals in Finder. `cd` in the focused pane, then
   use the header refresh (herdr has no live cwd push). Switch sessions and
   confirm the root follows selection. Drag the second divider and relaunch:
   the file sidebar resumes its width and visibility. Confirm a plain-shell
   session shows its OSC-7 cwd. Switch the header toggle to Changes (and
   ⌘K "Toggle File Sidebar Git Changes", leader `o g` from hidden, Files and
   Changes): only modified and new files show (no deleted files, no untracked `build/`-style folders), with M/A/R/U/! badges, directories
   expanded; edit a file in the terminal and it appears after the next key;
   a subdirectory root lists only its own changes; a non-repo directory says
   so. Relaunch: the mode is remembered.

10. Onboarding: quit Vakta, move `~/Library/Application Support/Vakta` aside,
    and launch the packaged app. The welcome tour appears over the main
    window; Next/Back/Skip, Return and Escape work; shortcut chips match
    Preferences (rebind ⌘K, reopen Help ▸ Welcome Tour, see the new chord;
    enable leader keys and see leader sequences). Close it, relaunch: nothing
    shows. Restore the original folder, delete only `onboarding.json`, and
    relaunch: What's New shows the latest note instead of the tour. Help ▸
    What's New and ⌘K "What's New in Vakta" reopen it without affecting
    what the next launch shows. On the tour's "Set up herdr and tmux" step,
    rows show installed paths; with one tool temporarily off PATH (e.g. a
    profile PATH or renamed binary), Install opens a new session that echoes
    and runs the command, reports the result, and closes on Return; Check
    Again (or refocusing the tour) updates the row. Quit mid-install and
    relaunch: the installer session is not restored.
11. Status bar (needs `gh auth login` and a repository with an open PR).
    Automatic (default): in a herdr and a tmux session, focus a pane on the
    PR's branch -- a bar appears under the terminal column only (sidebars
    keep full height) showing the branch, the PR glyph, and `#N`; hovering
    shows the title and state, clicking opens the PR. Focus a pane outside
    any repository: the bar stays about 10 s, then undocks. The terminal
    resizes only when the bar docks/undocks, and keeps its content (same
    surface). With checks failing on another pane's PR, the `⚠ N` count
    shows. Switch to a pane in another worktree: its branch/PR shows within
    a refresh (≤ 20 s). Return to the app after pushing a new PR elsewhere:
    it shows promptly. Auto-hide: nothing shows; rest the pointer at the
    terminal's bottom edge -- the bar fades in over the terminal without
    resizing it, stays while hovered, fades out after leaving; sweeping past
    quickly doesn't reveal it; `htop`/vim mouse mode in the bottom row still
    works while it's hidden. With a PR's checks going pending → failing, it
    peeks for ~3 s. Always: shows even outside a repository (empty). Never:
    no bar, and no `gh`/`git` helpers run (Activity Monitor). ⌘K "Cycle
    Status Bar Visibility" and leader `o v` step the modes, the Preferences
    ▸ Appearance picker follows, and the choice survives a relaunch; ⌘K
    "Open Pull Request" / `o r` opens the focused PR. ⌘K "Show Status Bar
    Briefly" / `o b` overlays the bar for ~25 s in Auto-hide, Automatic
    (when undocked), and Never (after a one-off lookup); pressing it again
    hides it; hovering holds it up. Light and dark terminal themes keep the
    text legible, and the Auto-hide overlay is opaque over terminal text.

Where automated desktop integration exists, record its results in place of the
equivalent manual step. Keep tests isolated from the user's live sessions.

## Coverage evidence and maintenance

Each behavior change needs a regression that fails for the intended reason before
implementation and passes afterward. Cover normal, empty, invalid, boundary, and
failure/recovery paths where applicable. For asynchronous work, include stale
completion and cancellation paths. For persisted changes, include old data and a
restart, not just encode/decode symmetry.

Report executed test names/suites and commands, remaining gaps, and desktop results.
Collect line/branch coverage after the harness exists to find missed paths; no
percentage target substitutes for these behavioral cases. Avoid tests for trivial
getters, generated project files, exact view trees, and private call ordering.
Update this matrix when a feature adds a new decision or effect boundary.
